import 'package:flutter/material.dart';

class FragmentWhislist extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Whislist'),
      ),
    );
  }
}
